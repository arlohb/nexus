apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cert-whoami
  namespace: default
spec:
  secretName: cert-whoami
  dnsNames:
    - whoami.arlodev.co.uk
  issuerRef:
    name: letsencrypt-staging
    kind: ClusterIssuer
