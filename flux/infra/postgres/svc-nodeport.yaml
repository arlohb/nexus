apiVersion: v1
Service
metadata:
  name: postgres
  namespace: default
spec:
  type: NodePort
  selector:
    apps/kubernetes.io/name: postgres
  ports:
    - port: 5432
      targetPort: 5432
      nodePort: 30432
