apiVersion: batch/v1
kind: Job
metadata:
  name: pg-create-extension-vector
spec:
  template:
    spec:
      nodeSelector:
        network: fast
      restartPolicy: Never
      containers:
        - name: pg-create-extension-vector
          image: ghcr.io/immich-app/postgres:18-vectorchord0.5.3-pgvector0.8.1
          env:
            - name: PGHOST
              value: postgres
            - name: PGPORT
              value: "5432"
            - name: PGDATABASE
              value: openwebui
            - name: PGUSERNAME
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: user
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: password
          command: 
            - sh
            - -c
            - psql
              --host=$PGHOST
              --dbname=$PGDATABASE
              --port=$PGPORT
              --username=$PGUSERNAME
              --command="CREATE EXTENSION vector;"
  backoffLimit: 4
