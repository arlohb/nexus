apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ollama-local
  namespace: default
spec:
  entryPoints:
    - local
  routes:
    - match: Host(`ollama.home`)
      kind: Rule
      services:
        - name: ollama
          port: 11434
