apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: prom
  namespace: default
spec:
  entryPoints:
    - local
  routes:
    - match: Host(`prom.home`)
      kind: Rule
      services:
        - name: kube-prometheus-stack-prometheus
          port: 9090
