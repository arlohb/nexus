apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: longhorn
  namespace: flux-system
spec:
  path: ./flux/infra/longhorn-config
  prune: true
  interval: 10m
  dependsOn:
    - name: longhorn
  sourceRef:
    kind: GitRepository
    name: flux-system
