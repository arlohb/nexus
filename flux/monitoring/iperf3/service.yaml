apiVersion: v1
kind: Service
metadata:
  name: iperf3
  namespace: default
spec:
  selector:
    app: iperf3
  type: NodePort
  externalTrafficPolicy: Local
  ports:
    - protocol: TCP
      port: 5201
      targetPort: 5201
      nodePort: 30521
