apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: open-webui-local
  namespace: default
spec:
  entryPoints:
    - local
  routes:
    - match: Host(`ai.home`)
      kind: Rule
      services:
        - name: open-webui
          port: 80
